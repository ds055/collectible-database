<content class = "flex flex-col">

  <div class = 'flex justify-center'>
    <h2 class = 'pb-5 font-bold text-4xl md:text-6xl lg:text-8xl m-6 bg-gradient-to-tr from-violet-700 via-fuchsia-300 to-teal-400 bg-clip-text text-transparent'>Your 
      {{#if figure}}
        Figures
      {{/if}}
      {{#if coin}}
        Coins
      {{/if}}
      {{#if card}}
        Cards
      {{/if}}
      {{#if music}}
        Music
      {{/if}}
    </h2>
  </div>
  <div class="flex felx-row justify-center">
        <button class="border-2 border-black bg-indigo-500 rounded-lg text-white text-lg px-0.5 me-3" id="addCollectionBtn">Add Collection</button>
        <button class="border-2 border-black bg-indigo-500 rounded-lg text-white text-lg px-0.5 me-3" id="addCoolectibleBtn">Add Coolectible</button>
  </div>
  <form id="view-all-form" class="flex justify-center items-center mt-5">
    <label class="font-bold text-lg" for="view-all">View All Your:</label>
        <select class="ms-3 me-4" id="view-all-choice" name="type-select">
        <option value="figure">Action Figures</option>
        <option value="card">Cards</option>
        <option value="coin">Coins</option>
        <option value="music">Music</option>
        </select> 
    <input class="cursor-pointer border-2 border-black bg-indigo-500 rounded-lg text-white text-lg px-0.5 me-3" type="submit">
  </form>
      {{!-- Modal Container --}}
    <div class="flex flex-row justify-center sticky inset-x-1/4 inset-y-1/4 z-50">
        <!-- Trigger/Open The Modal -->
        <!-- The Modal -->
        <div id="myModal" class="modal absolute hidden w-fit h-fit bg-indigo-200 border-4 border-indigo-600 rounded-md drop-shadow-2xl">
        <!-- Modal content Generated Here Dynamically from script-->
        </div>
    </div>

  <section class="text-white body-font my-4">
      <div class="px-10 py-16 mx-auto bg-white bg-opacity-70 rounded-lg">
          <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-6">
          {{#if coin}}

            {{>coin}}
          
          {{/if}}

          {{#if figure}}
            {{>figure}}
          {{/if}}
          
          {{#if music}}
            {{>music}}
          {{/if}}

          {{#if card}}
            {{>card}}
          {{/if}}
          
          </div>

      </div>

  </section>

</content>

<script src="/js/view-all.js"></script>
<script src="/js/edit-item-all-modal.js"></script>
<script src="/js/add-to-coll.js"></script>